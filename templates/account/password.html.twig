{% extends 'base.html.twig' %}


    {% block body %}
    
        <div class="container">
            <h1 class="mt-5 mb-5 text-center">Espace membre</h1>
 
            <div class="row">
                <div class="col-md-3 pt-3 text-primary-emphasis bg-primary-subtle border border-primary-subtle rounded-3 text-center">
                    {% include 'account/_menu.html.twig' %}
                </div>
                <div class="col-md-8 offset-1 border border-primary-subtle p-5 rounded-3">
                    <h5 class="mb-5 text-primary text-center">Modification de mon mot de passe</h5>

                    {{ form(modifyPwd)}}
                </div>
            </div>
        </div>
    {% endblock %}